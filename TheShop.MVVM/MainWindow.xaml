<Window x:Class="TheShop.MVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:TheShop.MVVM.View"
        xmlns:local="clr-namespace:TheShop.MVVM"
        mc:Ignorable="d"
        Title="The Shop" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>-->
        <view:NavigationView DataContext="{Binding NavigationViewModel}" Margin="0,-10,-153.2,10" Grid.RowSpan="2"/>
        <view:ProductDetailView DataContext="{Binding ProductDetailViewModel}" Margin="354,0,-381.2,0" Grid.RowSpan="2"/>
        <Menu HorizontalAlignment="Left" Height="51" VerticalAlignment="Top" Width="100" Margin="667,29,-566.2,0">
            <MenuItem Header="Create"/>
            <MenuItem Header="New Product" Command="{Binding CreateNewProductCommand}"/>
        </Menu>
    </Grid>
</Window>
